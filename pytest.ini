[pytest]
asyncio_mode = auto
# Turn a future deadlock (e.g. a stale global asyncio.Lock leaking across tests)
# into a per-test timeout failure instead of an infinite hang. ``thread`` works
# even when the asyncio event loop itself is wedged.
timeout = 60
timeout_method = thread
